# R-MoE v2.0 Python Engine — runtime dependencies
# =================================================
#
# Google Colab (GPU build — pre-built CUDA wheel, ~60 s install):
#   pip install llama-cpp-python \
#     --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121 \
#     --upgrade
#
# CPU-only (development / offline testing):
#   pip install -r requirements.txt
#
# Note: all other rmoe/ modules use only the Python standard library.
# No PyTorch, no transformers, no extra embeddings model required.

llama-cpp-python>=0.2.90
